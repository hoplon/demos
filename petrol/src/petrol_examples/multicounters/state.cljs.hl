(ns petrol-examples.multicounters.state
  (:require
    [petrol-examples.counter2.state :as counter2]))

(def initial
  {:counters [counter2/initial
              counter2/initial]})

(defonce !app
  (cell initial))
